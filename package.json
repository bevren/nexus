{
  "name": "simple-code-tui",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node index.js --self-test-append && node index.js --self-test-format && node index.js --self-test-execute && node --check index.js",
    "build": "esbuild index.js --bundle --platform=node --format=cjs --outfile=dist/bundle.js && node --experimental-sea-config sea-config.json && node -e \"require('fs').copyFileSync(process.execPath,'sea.exe')\" && postject sea.exe NODE_SEA_BLOB sea-prep.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2 && python -m PyInstaller --noconfirm --distpath dist tools_launcher.spec && copy /Y dist\\tools.exe tools.exe"
  },
  "dependencies": {
    "openai": "^6.25.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "postject": "^1.0.0-alpha.6"
  }
}
